import { Loader } from "src/shared/ui-kit/Loader";
import * as Styled from "./styles";

export const LoaderPage = () => {
    return (
        <Styled.Box>
            <Loader />
        </Styled.Box>
    );
};
