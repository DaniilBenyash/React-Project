import {
    ListItem as ListItemMUI,
    ListItemButton,
    ListItemText,
    Link as LinkMUI,
    styled,
} from "@mui/material";

export const Link = styled(LinkMUI)`
    text-decoration: none;
    color: black;
    width: 300px;
`