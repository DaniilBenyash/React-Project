import { Loader as LoaderMUI } from "src/shared/ui-kit";
import * as Styled from "./styles";

export const Loader = () => {
    return (
        <Styled.Box>
            <LoaderMUI />
        </Styled.Box>
    );
};
